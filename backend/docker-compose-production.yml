version: '3.3'

services:
  authentication-service:
    build: ./Authentication-service
    ports:
      - 8000:8080
    environment:
      - MODE=production

  interviewer-service:
    build: ./Interviewer-service
    ports:
      - 8001:8080
    environment:
      - MODE=production

  interviewee-service:
    build: ./Interviewee-service
    ports:
      - 8002:8080
    environment:
      - MODE=production

  notification-service:
    build: ./Notification-service
    environment:
      - MODE=production
  # process-interview:
  #   build: ./Process-interview
  #   volumes:
  #     - ./Process-interview:/app
